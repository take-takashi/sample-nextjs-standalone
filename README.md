# sample-nextjs-standalone

# setup app dir

```sh
mkdir app
cd app
# non-interactively command
npx create-next-app@latest . --ts --eslint --experimental-app --src-dir --use-npm --import-alias "@/*"
```
